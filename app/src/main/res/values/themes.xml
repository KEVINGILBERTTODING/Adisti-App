<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Adisti" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/main</item>
    </style>

    <style name="Theme.Adisti" parent="Base.Theme.Adisti" />

    <style name="style_splash_screen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="TabFont" parent="TextAppearance.Design.Tab">

        <item name="android:fontFamily">@font/popmed</item>

        <item name="android:textSize">@dimen/_11sdp</item>

        <item name="textAllCaps">false</item>
    </style>

    <style name="BottomNavigationTitleStyle" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/main</item>
        <!-- Anda juga dapat menambahkan atribut lain untuk mengkustomisasi judul -->
    </style>

</resources>